# Phase 04-01: UI 고도화 및 잔여 영역 데이터 연동

> **목표:** Phase 04의 MVP 기능 구현 이후, 프론트엔드에 남아있는 불필요한 더미 데이터를 완벽히 제거하고 사이드바 통계 연동 및 우측 패널의 스크롤/UI 이슈를 고도화합니다.

## ✅ 체크리스트

### 1. 프론트엔드 더미 데이터 청소 및 사이드바 연동
- [x] 소스코드 내 불필요하게 남아있는 목업/더미 데이터 파일 또는 변수 식별 및 제거
- [x] 좌측 `Sidebar`의 '내 보험 요약' 영역에 `useGetInsurances` API 데이터를 연동하여 실제 통계(총 보험 개수, 월 총 납입액 등)가 노출되도록 변경

### 2. 우측 상세 패널(Right Panel) 스크롤 및 UI 고도화
- [x] 폼 모드(`InsuranceForm`) 또는 뷰 모드 진입 시 콘텐츠가 잘리거나 스크롤이 비정상적으로 동작하는 문제 해결
- [x] 최하단 플로팅 버튼(저장/추가 버튼)이 콘텐츠를 가리지 않도록 하단 여백(padding) 조정 및 스크롤 영역 분리

### 3. 상단 대시보드(Stat Cards) 데이터 연동 (옵션)
- [x] `HomePage.tsx` 상단의 StatCards 영역에 실제 API 데이터를 기반으로 누적/월간 통계 산출 로직 적용 (선택적 진행)

### 4. 테스트 코드 유지보수
- [x] 컴포넌트 수정에 따라 깨지는 테스트 코드(Vitest)가 있다면 즉각 수정하고 커버리지 유지
