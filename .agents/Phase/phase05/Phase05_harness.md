# Phase 05: 내 보험 (My Insurance) 리스트 뷰 및 관리 기능

> **목표:** 와이어프레임(`Design&Wireframe.svg`) 구조에 완벽히 부합하도록, 중앙 캘린더를 유지한 채 좌측 사이드바 메뉴 클릭 시 **우측 패널(RightPanel) 내부**에서 '내 보험' 전체 리스트를 확인하고 관리할 수 있는 기능을 구현합니다. 기능 구현 시 반드시 단위 테스트를 동반하여 커버리지를 확보합니다.

## ✅ 체크리스트

### 1. 전역 상태 및 사이드바 전환 연동 (테스트 포함)
- [x] `useAppStore`의 `panelMode` 타입에 `'my-insurance'` (또는 `'list'`) 모드 추가
- [x] 좌측 `Sidebar` 컴포넌트의 '내 보험' 메뉴 아이템 클릭 시 `setPanelMode('my-insurance')` 호출 로직 구현
- [x] 🧪 **[Test]** `useAppStore.test.ts`에 새로운 상태 전환 로직 단위 테스트 추가
- [x] 🧪 **[Test]** `Sidebar.test.tsx`에서 '내 보험' 메뉴 클릭 시 상태 변경 함수가 올바르게 호출되는지 검증

### 2. 우측 패널 리스트 컴포넌트(`MyInsuranceList`) 구현 (테스트 포함)
- [x] `RightPanel.tsx` 내에서 `panelMode === 'my-insurance'`일 때 분기하여 렌더링될 `<MyInsuranceList />` 컴포넌트 생성
- [x] `useGetInsurances` 데이터를 구독하여 전체 보험 목록을 와이어프레임 시안에 맞게 카드 형태로 세로 나열 렌더링 (보험명, 카테고리, 월 납입액 요약 정보)
- [x] 🧪 **[Test]** `<MyInsuranceList />`가 전달받은(또는 Mocking된) 데이터를 기반으로 정상 렌더링되는지 (Vitest + RTL) 단위 테스트 작성
- [x] 🧪 **[Test]** 가입한 보험이 없을 경우(Empty State) 안내 문구가 노출되는지 테스트

### 3. 리스트 내 필터링 및 관리 인터랙션
- [x] 리스트 상단에 보험 종류별 카테고리(예: 전체, 실비, 건강 등) 필터링 UI 구현 및 동작 연동
- [x] 개별 보험 카드의 '상세 보기(수정)' 버튼 클릭 시 기존의 폼 패널 모드(`form`)로 전환 및 선택된 보험 데이터를 폼에 주입하는 로직 연결
- [x] 🧪 **[Test]** 카테고리 필터 클릭 시 리스트가 올바르게 필터링되는지 통합 테스트 작성

### 4. 엣지 케이스 및 레이아웃 안정성 점검
- [x] 스크롤 바운더리 체크: 리스트가 길어질 경우 우측 패널 내부(`flex-1 overflow-y-auto`)에서만 스크롤이 발생하도록 구조 검증
- [x] 캘린더 뷰 복귀 확인: 중앙 메인 캘린더의 날짜를 다시 클릭했을 때, 우측 패널이 즉각적으로 `'view'`(해당 일자 스케줄) 모드로 돌아오는지 로직 점검 및 통합 테스트
